# This function is here to great the data summary (expand)

remove(pca2)
str(data)


# graph = factor - group
# variables on x + y axis = 

# from visit 1 to visit 2, change in x between demented, non demented and converted
# can CDR rating be rused instead?


# possibly filter into seperate datasets?
# newdata <- data %>%
    # filter(year == 2007)
# if want to add new filter add coma and then new constraint


# creating new column
# data %>%
#    mutate(newcolumn = existing * existing)


# possibly change values so easier eg value from yrs to months


